package network;


public class ClientServerCombo {

 /*public static void main(String[] args) {
    String ipAddr = null;
    if (args.length != 0) {
    	ipAddr = args[0];
    }

    if (ipAddr == null) {
    	Server theServer = new Server();
    	Thread serverThread = new Thread(theServer);
    	serverThread.start();
    }
    else  {
    	Client theClient = new Client(ipAddr);
    	Thread clientThread = new Thread(theClient);
    	clientThread.start();
    }
  }*/

}